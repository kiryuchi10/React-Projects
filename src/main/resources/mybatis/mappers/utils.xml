<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.projectD.repository.dao.UtilDao">
    <select id="findAllOrders" resultType="com.demo.projectD.repository.vo.UtilVo">
        SELECT 
            order_id AS orderId,
            branch_id AS branchId,
            order_date AS orderDate,
            checked AS checked
        FROM book_order
    </select>

    <select id="findOrdersByDate" parameterType="String" resultType="com.demo.projectD.repository.vo.UtilVo">
        SELECT 
            order_id AS orderId,
            branch_id AS branchId,
            order_date AS orderDate,
            checked AS checked
        FROM book_order
        WHERE order_date = #{date}
    </select>
</mapper>
